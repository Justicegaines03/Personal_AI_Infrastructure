# 2026-03-08

## Session Log

- Built automatic upstream PAI sync system:
  - `scripts/pai-sync.sh` — fetches upstream, merges into main, detects new releases, repoints ~/.claude symlinks
  - Dry-run by default (--apply to execute), timestamped backups, protects user-owned files
  - `scripts/install-sync.sh` — installs macOS launchd job for daily 06:00 sync
  - `scripts/com.pai.upstream-sync.plist` — launchd service definition
- Ran first sync: upgraded v4.0.1 → v4.0.3 (6 upstream commits merged)
  - v4.0.2: 11 bug fixes, Linux compatibility
  - v4.0.3: 4 community PR fixes, installer merge logic fix
- Installed launchd job `com.pai.upstream-sync` — verified active
- Added HEARTBEAT.md check (#5) for sync health awareness
- Previous symlink targets backed up to `~/.claude/backups/pre-v4.0.3-*`

## Architecture Notes

- Symlinks in ~/.claude point into `Releases/<version>/.claude/` inside the PAI repo
- User-owned files (SOUL.md, USER.md, MEMORY.md, HEARTBEAT.md, IDENTITY.md, AGENTS.md, TOOLS.md, CLAUDE.md) are never touched by sync
- Sync flow: fetch upstream → merge into main → detect latest Releases/vX.Y.Z → repoint symlinks → log
- Backup of old symlink targets stored before each upgrade
